# Cargo rebuilds the development executable on nearly every Tauri run. macOS
# otherwise gives that binary a new ad-hoc identity, so Little Snitch asks to
# accept the "modified" application after every code change. Use a stable
# Apple Development signature before launching on macOS.
[target.'cfg(target_os = "macos")']
runner = "../scripts/run-signed-dev.sh"
